syntax = "proto3";

package smoke;

// Minimal message used only to verify protobuf code generation + linking.
message SmokePing {
  string name = 1;
  int32  count = 2;
  repeated string tags = 3;
}
